DATABASE_URL=mysql://root:YOUR_PASSWORD@localhost:3306/automation_testing

# 🚀 Phase 7: 浏览器预安装优化 - 设为false可跳过预安装检查，提升启动速度
PLAYWRIGHT_PRE_INSTALL_BROWSER=false

# 🤖 AI服务配置 (OpenRouter)
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
DEFAULT_MODEL=openai/gpt-4o
DEFAULT_TEMPERATURE=0.3
DEFAULT_MAX_TOKENS=4000

# 🤖 本地 Gemini API 配置（可选）
# 用于本地部署的 Gemini 2.5 Flash 模型，支持文本、图片、音频多模态输入
GEMINI_LOCAL_BASE_URL=http://localhost:3000/v1
GEMINI_LOCAL_API_KEY=your_local_api_key_here

# 🤖 本地 Qwen API 配置（可选）
# 兼容 OpenAI / Claude 协议的 Qwen Turbo 服务，推荐使用 OpenAI 风格端点
QWEN_LOCAL_BASE_URL=http://localhost:3000/openai-qwen-oauth/v1
QWEN_LOCAL_API_KEY=your_qwen_api_key_here
# 请求示例（OpenAI 协议）：
# curl http://localhost:3000/openai-qwen-oauth/v1/chat/completions \
#   -H "Authorization: Bearer ${QWEN_LOCAL_API_KEY}" \
#   -H "Content-Type: application/json" \
#   -d '{"model":"qwen-turbo","messages":[{"role":"user","content":"Hello!"}]}'

# 安全提示：
# - 将 GEMINI_LOCAL_API_KEY / QWEN_LOCAL_API_KEY 替换为实际的认证密钥
# - 生产环境请使用强密码，避免使用默认值（如 "123456"）
# - .env 文件已在 .gitignore 中排除，不会提交到版本控制
# - 本地 API 服务需先启动（默认端口 3000）

# 🌐 代理配置（用于访问国外 API）
HTTP_PROXY=http://127.0.0.1:10808
HTTPS_PROXY=http://127.0.0.1:10808

# ========== Qdrant向量数据库知识库配置 ==========
QDRANT_URL=http://localhost:6333

# Embedding提供商选择: gemini, openai, 或 aliyun（推荐国内用户）
EMBEDDING_PROVIDER=aliyun

# 方案1：阿里云通义千问 Embedding（国内推荐！）
# 获取API Key: https://dashscope.console.aliyun.com/apiKey
EMBEDDING_API_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBEDDING_API_KEY=your_aliyun_api_key_here
EMBEDDING_MODEL=text-embedding-v4

# 方案2：Google Gemini Embedding（完全免费，需代理）
# EMBEDDING_PROVIDER=gemini
# GEMINI_API_KEY=your_gemini_api_key_here

# 方案3：OpenAI Embedding（如果有API Key）
# EMBEDDING_PROVIDER=openai
# EMBEDDING_API_BASE_URL=https://api.openai.com/v1
# EMBEDDING_API_KEY=your_openai_api_key_here
# EMBEDDING_MODEL=text-embedding-3-small
